/**
 * Frontend Configuration
 *
 * 이 파일을 실제 환경에 맞게 수정하세요.
 * Edit this file for your environment.
 */
window.APP_CONFIG = {
    // Clerk 설정
    // Clerk configuration (get from https://clerk.com)
    CLERK_PUBLISHABLE_KEY: 'pk_test_your_publishable_key',

    // API Base URL (비워두면 현재 호스트 사용)
    // Leave empty to use current host
    API_BASE_URL: '',

    // WebSocket URL (비워두면 현재 호스트 기반으로 자동 설정)
    // Leave empty for auto-detection based on current host
    WS_URL: ''
};
